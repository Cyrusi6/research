# /status

Read `workspace/{project_id}/meta/registry.yaml` and the stage manifests, then print:

1. current stage and iteration
2. stage statuses and artifact counts
3. active blocked reason, if any
4. latest review score and decision, if present
5. next recommended action
