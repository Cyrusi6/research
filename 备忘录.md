s2.5是一次性非交互任务，并且在临时的repo上跑
短期用从“一次性 codex exec”改成 Codex persistent session backend，每个 idea 建一个稳定 worktree（就用 Git worktree） + persistent Codex session，之后用 codex exec resume 继续同一个会话。这样它会更像“同一个工程师连续修同一个 patch”。
每个 idea 一个 session，不要全局一个 session。
每个 idea 一个稳定 patch worktree，不要每次 temp dir。
第 1 次：context preload，只读仓库，输出 patch blueprint。
第 2 次：resume，同 session 写 patch。
第 3 次以后：resume，带 validation/runtime smoke traceback 做窄修复。
每次调用用 --json 记录事件，配 no-progress watchdog（这个先看看第一次的时间再决定做限时）。
长期用ACP/acpx 这类 persistent agent bridge，它通过 acpx 建 named session，后续所有代码生成调用都复用这个 session，并带 heartbeat。

  冗余最明显的是这几个:
  experiment_designer、systems_feasibility、skeptic_reviewer 


    推荐设计：

  S0:
  - 抽取所有 paper / rebuttal / code
  - 全部分块
  - 每个 chunk 有 chunk_id、source_path、section、text、keywords
  - 生成轻量 evidence_brief + 完整 chunk_index
  重点分析元素设计
我们先把“输入源 -> 干净的中间态”这条链路跑通，然后再考虑后面切分
  对于 PDF：用MINERU.md里面的教程使用mineru，然后我api已经配置到本地，MINERU_API_KEY=这个。我需要你调用这个api，能稳定地把PDF转化为带有完整 #, ## 层级和 LaTeX
  公式块的 paper_full.md。

    输出位置：
  workspace/mineru_pdf_intake_20260603/

  关键文件：

  - workspace/mineru_pdf_intake_20260603/paper_full_manifest.json

对于 Code：引入 tree-sitter (Python 库叫 tree_sitter 和 tree_sitter_python)。
• 我查了一圈，代码切块最适合你的方案不是“固定长度切 chunk”，而是：

  Tree-sitter AST 切块 + repo map + 轻量关系图 + 按需取完整代码。

  原因很直接：代码不是自然语言，最重要的是函数/类/调用关系/可编辑行号。纯文本 chunk 或 embedding chunk 会丢结构，后面 S2/S2.5 写 patch 时容易改错位置。

  我的推荐方案：

  S0 Code Intake
  1. 扫描 repo
  2. Tree-sitter parse Python 文件
  3. 提取 symbol chunks
  4. 提取 relations
  5. 生成 repo map
  6. 生成 code_chunk_index

  中间态产物建议

  intake/c2c/code_file_manifest.json
  intake/c2c/code_symbols.jsonl
  intake/c2c/code_chunks.jsonl
  intake/c2c/code_edges.jsonl
  intake/c2c/code_repo_map.json
  intake/c2c/code_repo_map.md

  chunk 类型

  1. file_prelude
      - imports
      - module docstring
      - global constants
      - config defaults

  2. class_skeleton
      - class signature
      - docstring
      - fields
      - method list
      - 不放完整大 class，避免太长

  3. function_body
      - function/method 完整代码
      - signature
      - decorators
      - docstring
      - start/end line
      - calls/imports/referenced names

  4. large_function_slice
      - 如果函数太长，再按内部 block 切
      - 每个 slice 都带函数 signature 和父级信息

  5. config_chunk
      - JSON/YAML 配置按 key path 切
      - 例如 model.cache_routing_mode

  6. test_chunk
      - test function
      - fixtures
      - assertions
      - inferred target symbol

  每个 code chunk 的字段

  {
    "chunk_id": "rosetta/model/aligner.py::C2CAligner.forward::120-210",
    "source_type": "code",
    "language": "python",
    "path": "rosetta/model/aligner.py",
    "node_type": "function_definition",
    "symbol": "C2CAligner.forward",
    "parent_symbol": "C2CAligner",
    "start_line": 120,
    "end_line": 210,
    "start_byte": 4200,
    "end_byte": 9800,
    "signature": "def forward(...)",
    "decorators": [],
    "docstring": "...",
    "imports": ["torch", "..."],
    "calls": ["project", "align_spans"],
    "references": ["valid_mask", "cache_states"],
    "config_keys": ["cache_routing_mode"],
    "risk_tags": ["alignment_core", "training_path"],
    "edit_surface": "allowed | risky | forbidden",
    "keywords": ["cache", "alignment", "valid_mask"],
    "text": "... exact code ...",
    "text_preview": "...",
    "tokens_estimate": 900,
    "sha256": "..."
  }

  edges 关系图

  {
    "edge_type": "calls",
    "src": "rosetta/model/wrapper.py::forward",
    "dst": "rosetta/model/aligner.py::align",
    "confidence": "static_name_match"
  }

  关系类型建议：

  contains
  imports
  calls
  references
  defines
  reads_config
  tested_by
  same_file_neighbor

  后续调用方式

  S1 不直接读完整代码，只读：

  code_repo_map.md
  code_file_manifest.json
  top code_symbols

  S2 需要计划时，按机制方向检索：

  query: cache routing / valid_mask / aligner / projector
  返回:
  - top symbol chunks
  - one-hop callers/callees
  - tests/config chunks

  S2.5 写 patch 时，再给它完整 edit bundle：

  target function full body
  parent class skeleton
  file prelude imports
  related config chunk
  related test chunk
  allowed edit surface

  我建议的技术选择

  第一版直接用：

  tree-sitter
  tree-sitter-python 

  不要一开始上 LSP / pyright / embeddings。原因是你现在目标是“干净中间态”，Tree-sitter 已经够稳定、可复现、无模型成本。LSP 可以作为第二版补充类型信息和真正的
  go-to-definition。

  最终推荐结构

  S0 Code Intake = Tree-sitter structural index
  S1 = 看 repo map 定方法方向
  S2 = 从 code index 检索相关 symbol
  S2.5 = 拿 exact edit bundle 写 patch
  S3 = 根据 patch touched symbols 做风险归因

  这比现在简单按文件/函数切更强，因为它保留了“代码可修改所需的信息”：符号、行号、调用关系、配置关系、测试关系。下一步实现时，我建议先只做 Python + JSON/YAML，
  正好覆盖 C2C 当前主要代码面。

  S1:
默认走 Codex resume evidence agent，
给 Codex 准备可读、可检索、可审计的本地证据库：
  - paper_full.md：让 Codex 不用读 PDF，直接读干净 Markdown。
  - paper_chunks/rebuttal_chunks/code_chunks：让 Codex 可以用 rg 精确搜证据，不用全仓库乱翻。
  - chunk_index.json/jsonl：给它一个目录，知道有哪些证据可读。
  - code_repo_map.md：让它快速了解代码结构。
  - implementation_surface_map.json：告诉它哪些机制面可改、哪些是高风险。
  - code_edges.jsonl：帮它判断函数、配置、测试之间的关系。
  - cache_summary/code_intake_report：让它知道 S0 证据质量和缓存状态。
  - MinerU/code 缓存：避免每次 resume 或重跑 S0 都重新解析。


  S2
  在同一个 resume session 里：

  - 读取 S1 direction
  - 按 direction 自动检索相关 code chunks
  - 如果 plan 不确定，继续请求更多证据
  - 输出具体实验计划和 implementation targets。

  S2.5
  Codex patch 阶段仍然可以自己读 repo，但 patch contract 里给它前面检索到的证据和目标 chunk，减少它乱找的成本。





  P1：S2.5 patch 质量评分还可以更偏 effect-first

  你之前提的“机制证据质量”现在先降权是对的。当前阶段最重要的是找到有效 patch。

  我建议 S2.5 排序优先级是：

  1. 能运行
  2. 不碰 evaluator
  3. cheap proxy 有正收益
  4. diff 小且风险低
  5. ablation/coverage 完整度

  论文解释性可以后置，不要在现在杀掉潜在有效 patch。


  总结一下：现在最大的问题不是框架方向，而是三个细节还需要增强：

  1. S1 证据引用要可验证
  2. S2 repair/new variant 决策要更准
  3. cheap proxy 和 full train 的相关性要持续校准

  我建议下一步先做 S1 evidence ref resolver + S2 repair/variant decision policy，这两个
  最直接提升自动科研闭环的稳定性。


  • 我觉得现在不要再大改框架主干了。你现在最缺的不是更多阶段，而是让真实循环更稳定地产生“有效 patch”。优先级我会这样排：

  P0

  1. 跑真实 S0-S3 闭环，观察失败分布
     现在 S0/S1/S2/S3 结构已经比较完整，继续堆设计收益不大。下一步应该看真实失败主要卡在：
      - S1 方向不对
      - S2 计划太虚
      - S2.5 写不出可跑 patch
      - cheap proxy 太严/太松
      - full train 与 proxy 不相关

  2. S2.5 patch 成功率继续优化
     这是当前最关键瓶颈。目标不是论文解释，而是：
      - 能 apply
      - 能 import
      - 能 smoke train 首 batch
      - 不碰 evaluator
      - cheap proxy 有正收益
        可以继续强化 runtime smoke 和 repair，但不要让质量 gate 太重。

  3. S3 cheap proxy 校准
     每次 proxy pass 但 full train fail，都要记录 false positive。否则 proxy 会慢慢筛错方向。

  P1
  4. S0 增强覆盖报告更直观
  现在 S0 已经支持 MinerU、tree-sitter、DeepSeek enrichment、cache merge。还可以加一个 S0 status：

  - paper/rebuttal/code 各有多少 chunk
  - DeepSeek 增强覆盖率
  - fallback 比例
  - cache hit/miss
  - 哪些高优先级 code chunk 没增强

  5. S1 evidence trace 更硬
     现在有 evidence ref resolver，但还可以要求每个关键 claim 附：
      - chunk_id
      - source_path
      - text_hash 或 line anchor
      - claim 是 support 还是 risk
        这样后面论文阶段不会发现 S1 引用了“看似合理但不够扎实”的证据。

  6. S2 方向内迭代记忆更精炼
     5 次同方向尝试后，不要只给 S1 日志，应该给一个 direction scorecard：
      - best proxy delta
      - best dataset delta
      - runtime pass rate
      - patch risk
      - 常见失败类型
      - 是否全数据集崩
        这个比原始日志更适合 S1 换方向。

  我最建议你现在做的顺序：

  先不继续加复杂功能，直接跑一轮真实 S0-S3。跑完后我们只看三件事：

  1. S2.5 有没有稳定写出 patch
  2. cheap proxy 有没有筛出有希望的 patch
  3. proxy pass 的 patch 到 full train 是否靠谱

  如果这三件事不稳，优先优化 S2.5 和 S3；如果这三件事已经稳，再开始优化 S1 创新性和后面的论文解释阶段。


  这次观察结论很明确：S2.5 valid patch 通过，S3 cheap proxy 运行并把它判为
  repairable risk，然后自动回到 S2 做 patch repair。现在正在跑修复版 patch
  的 Codex session。这个正好是你想验证的链路。
  为什么要到s2里面进行修复，s2只判断方向
  



  上一轮最重要的结果是：框架方向是对的，已经能自动筛掉坏 patch、归因失败、重置 S1 方向；但当前还处在“发现机制还不够强”的阶段。它现在更像一个可靠的负样本筛选器，还不是高命中率的有效 patch 搜索器。

P0：真实跑 1-2 轮，校验新 S2 variant 搜索                                 
  重点看：                                                                  
                                                                            
  - plan/variant_candidates.json 是否真的生成 3-5 个不同 variant            
  - scorer 是否选中了合理的 1-2 个                                          
  - S2.5 是否按 variant_fingerprint 写 patch                                
  - 高风险 projector/evaluator 之类是否被降权                               
  - cheap proxy 失败后，是修 patch、换 integration point，还是回 S1，路由是 
    否合理                                                                  
                                                                            
  这是现在最关键的验证。                                                    
                                                                            
  P1：S2 scorer 校准                                                        
  现在 scorer 是规则启发式，能用，但还没被真实结果校准。后面每次 cheap/full 
  结果出来，都应该记录：                                                    

  - variant score
  - cheap proxy delta
  - full train delta
  - 真实失败原因
  - scorer 选中是否合理

  然后再调权重。否则 scorer 可能看起来聪明，但实际偏向错误 variant。

  P1：S2.5 patch 质量监控
  现在 contract 已经要求实现 s2_variant，但还可以做一个轻量 post-patch 检
  查：

  - 是否真的改了 selected variant 的 expected files
  - 是否碰了不该碰的 risk files
  - 是否 ablation switch 被 wire 上
  - 是否 patch 实际体现 integration_point / control_signal

  这个建议做成 soft score，不要硬 reject，避免又把有效 patch 杀掉。

  P1：cheap proxy/full train 相关性
  这是科研质量核心。只要出现 cheap proxy 过了但 full train 不好，就记录
  false positive：

  - 哪个数据集 proxy 预测错
  - 哪种机制 proxy 好但 full 差
  - proxy delta 和 full delta 差多少

  后面可以反过来优化 cheap proxy，不然它可能筛错方向。

  P2：监控报告更直接
  现在 artifact 很多，建议继续增强 auto-research report：

  - 当前 S1 direction
  - 同方向第几次
  - S2 variants 和分数
  - S2.5 selected fingerprint
  - patch risk
  - proxy delta
  - 下一步路由

  这样你不用每次翻 JSON。




  P0：共享池检索不要全塞 prompt

  现在共享池能进 S1/S2，但后面多了以后不能全部塞。应该做 top-k retrieval：

  - 按 task/topic
  - 按 dataset，例如 mmlu-redux
  - 按 mechanism_type
  - 按 failure mode
  - 按 source repo fingerprint

  这样 S1 只看最相关的失败经验，不会被无关项目污染。




• 我同意你的方向：现在 gate 里有一部分在做“质量评审”，这不适合无人值守 discovery。更好的设计是让 gate 只管安全和可
  执行，其余交给 Codex repair、proxy 结果和记忆池学习。

• 我同意。现在更合理的方向是：gate 轻量化，Codex 负责修，proxy/full train 负责裁决效果。

  静态 gate 不应该判断“是不是好 idea / 好机制”。它只能判断几件硬事实：

  能不能运行
  有没有污染评测
  有没有改数据/权重/历史结果
  有没有明显 no-op
  有没有外部错误比如 429

  除此之外都不应该硬挡。

  我建议改成两种模式

  gate_mode: discovery   # 默认，用来找有效 patch
  gate_mode: strict      # 后期论文版/收敛版再用

  Discovery 模式
  目标是最大化找到有效 patch。

  硬挡只保留：

  1. patch 完全没有 executable change
  2. 语法错误 / import 错误 / runtime smoke 跑不起来
  3. evaluator / metric / dataset / checkpoint / baseline 污染
  4. Codex 429 / quota，这个进入 retryable paused，不算失败
  5. eval 输出完全坏掉，比如预测为空、answer parser 全失败

  其他全部变成 soft warning：

  - patch 太宽
  - helper 文件多
  - ablation wiring 不完整
  - coverage diagnostics 不完整
  - matched coverage 不完整
  - 机制证据不够漂亮
  - 文件改动不够优雅
  - test 不够完整

  这些不要挡住 S3 cheap proxy，只记录成：

  "quality_debt": [
    "patch_too_broad",
    "ablation_weak",
    "coverage_missing"
  ]

  只要能跑、没污染、proxy 有希望，就继续。

  Strict 模式
  等发现有效 patch 之后再打开，用来论文化和收敛：

  - 文件数限制变硬
  - ablation 必须完整
  - coverage / matched coverage 必须完整
  - mechanism evidence map 必须完整
  - patch 要更小、更干净、更可解释

  也就是说：

  discovery 找效果
  strict 做论文质量

  不要在 discovery 阶段用 strict gate 杀 patch。

  S2.5 的正确职责
  S2.5 不应该被静态 gate 反复拦死。它应该是：

  Codex 写 patch
  -> 轻量安全检查
  -> 有问题自动 repair
  -> repair 不行就换同方向 variant
  -> 能跑就交给 proxy

  不是：

  Codex 写 patch
  -> 静态 gate 嫌不够漂亮
  -> 卡住

  我觉得最好的无人值守策略

  1. gate 只输出三类结果：

  PASS              继续跑
  AUTO_REPAIR       自动回 S2.5 修，不问人
  PAUSE_RETRYABLE   429 / quota / 环境问题，暂停等待恢复

  尽量不要用 FAIL 卡死人。

  2. quality 问题不挡流程，只扣分：

  quality_score
  risk_labels
  quality_debt
  repair_suggestion

  3. S3 cheap proxy 才是第一裁判：

  能跑 + 无污染 + proxy 有正收益/混合信号 = 继续
  能跑 + proxy 明显差 = method failure
  不能跑 = implementation failure，只回 S2.5

  4. full train 前只做最低安全确认：

  no evaluator contamination
  baseline/proxy config 可复现
  runtime smoke passed
  eval smoke healthy

  不要要求 paper-level diagnostics。

  一句话
  你现在要的是 effect-first discovery，所以 gate 应该是“安全护栏”，不是“质量评审委员会”。质量评审应该后移到有效
  patch 找到之后。Codex 可以负责实现和修复，proxy/full train 负责判断效果，gate 只防止污染和跑不起来。


    1. S3 候选执行和 patch_manifest 不同步。
     第一轮 manifest selected 是 pathology_triggered_prior_fallback，但 S3 也跑了
     local_monotonic_neighbor_window_router。第二轮 manifest selected 是 ambiguous_row_soft_prior_router，但 S3 又
     开始跑旧的 local_monotonic / pathology。这是当前最大流程 bug。



• 我看了最近两轮真实 artifact。结论是：S2.5 现在“能写出可运行形状”，但还没稳定做到“机制真实闭环”。

  质量大概这样：

  - 可运行性：中上
    py_compile、focused pytest、first-batch train 经常能过，说明 Codex 不是完全写不出代码。

  - 机制完整性：中等偏弱
    例如 workspace/c2c_lightgate_two_round_20260610/plan/code_patches/ambiguous_row_soft_prior_router/validation.json 是 status=ok，但有：
      - mechanism_activation_wiring_failed
      - ablation switch 没有被 forward 读取
      - 新参数没有进入 config_overrides

  - patch 复杂度：偏高
    最近几个 patch 经常 6-7 个文件、800-1000 行 diff。比如 ambiguous_row_soft_prior_router 改了 6 个文件，825 行；pathology_triggered_prior_fallback 到 1057 行。这说明它倾向
    一次性写完整机制、诊断、测试、训练接线，成本和失败面都偏大。

  - 实验可信度：之前偏危险，现在已改善
    之前 light gate 会把 ablation wiring failure 当 quality debt 放过去。我们刚加的 forward-level activation probe 会把这类问题前置挡住，后面应该不会再让“开关没接入
    forward”的 patch 直接进 full S3。

  我觉得下一步最值得优化的是这几个：



  P0：限制第一版 MVP patch 面积

  让 S2.5 第一版只做：

  - 最多 1 个 helper 文件；
  - 最多 2 个 runtime integration 文件；
  - 1 个 focused test；
  - 不要一开始同时改 aligner、projector、wrapper、train、dataset_adapter。

  现在它一口气改太多，容易出现“看起来完整，但没有一个路径真的闭环”。更好的策略是先把最小 effect path 跑通，再补诊断/coverage。

  P1：让 S2 输出 integration contract 更具体

  S2 不只是给 idea，还要明确：

  - primary integration point；
  - 必须被哪个 forward 调用；
  - 哪个 config key 激活；
  - ablation off 时应该回到哪个 baseline path；
  - 哪个 tensor/field 应该变化。

  这样 S2.5 不会自由发挥成大 patch。

  P1：测试不能只由 patch 自己写

  现在 focused test 很多是 Codex 自己加的，容易“自证”。建议框架生成一两个 contract test：

  - ablation switch on/off 是否走不同 branch；
  - new config key 是否能从 eval config 传到 projector/wrapper；
  - enabled/disabled 的 forward probe 是否变化。

  Codex 可以补测试，但核心 contract test 应该由系统生成。

  P1：best-of-N scorer 加强惩罚

  现在 scorer 给 validation_ok 和 runtime_smoke_ok 加分太多，导致带 quality debt 的大 patch 还能高分。建议强扣：

  - mechanism_activation_wiring_failed
  - 核心参数未激活
  - diff > 700 行
  - changed files > 5
  - 新 helper 未被 forward/loss 调用
  - 只在测试里证明机制

  总体评价：S2.5 已经从“写不出代码”进化到“能写出机制样子并跑首 batch”，但现在主要短板是真实执行路径和 ablation 路径不够可靠。下一轮优化应该优先围绕“闭环可验证的最小 patch”，
  而不是继续放宽 gate。


  • 我觉得现在最值得优化的不是再加 gate，而是三件事：

 

 

  P0：full S3 前加一个更便宜的中间阶梯
  现在 cheap proxy 和 6 卡 full train 中间跨度还是大。可以加 mini-full：比如更小 epoch / 更少样本 / 1-2 卡 / 全数据集 eval 子集。通过 cheap proxy 后先跑 mini-full，只有仍有
  正信号才进完整 S3。这个能降低 proxy 假阳性带来的成本。

  然后是 P1：

  - S2 variant scorer 继续强化：如果同一 integration point 连续失败，自动降低权重，但不硬禁。
  - proxy/full calibration 继续积累：哪些机制 proxy 好但 full 差，要长期写入 shared memory。
  - report 更聚合：一眼看到当前 patch 是否 eligible for S3、卡在哪里、下一步是 S2.5 repair 还是 method variant。
  - novelty checker 可以稍微放宽，只拦明显重复，不要杀掉有潜力但表达相似的 idea。

  我建议下一步优先做第一个：repo 专用 forward activation probe。这是最能减少“代码写了但机制没真生效”的浪费，而且不限制 Codex 创造力。
  

  full train 失败后的 checkpoint resume 更强
  如果 6 卡训练中断，下一次 resume 应该优先识别已有 checkpoint，而不是重新从头跑。
     个方向已经对，后面可以继续强化质量评分。\





  


  
  P1：cheap proxy/full train 相关性继续做实

  现在机制接通检查更强了，下一步要看：

  - probe passed 但 proxy failed；
  - proxy passed 但 full failed；
  - 哪类机制假阳性最多；
  - 哪个 dataset proxy 最不准。

  这会决定 cheap proxy 是否需要按 dataset/机制类型调权重。

  P1：S2 variant scorer 结合 activation 历史

  如果某个 integration point 连续出现 activation no-op，比如只改 config 不进 forward，可以给 S2 降权：

  - repeated no-op integration point；
  - repeated tensor-identical mechanism；
  - repeated config-only patch；
  - repeated projector-only but wrapper not wired。

  这样 S2 会更倾向提出能真实接入 runtime path 的 variant。

  我建议下一步先做两个：wrapper-level activation probe 和 tensor_checks 驱动的 S2.5 repair packet。这两个最直接减少“代码写了但机制没生效”的 token 浪费。


  



  P1：显式 patch eligibility artifact

  建议生成一个统一的：

  plan/code_patches/patch_eligibility.json

  里面明确：

  - patch_json ok；
  - evaluator not touched；
  - runtime smoke ok；
  - activation wiring ok；
  - forward probe ok；
  - eval smoke ok；
  - ready_for_s3 true/false；
  - blocker_reason。

  S3 只看这个 eligibility artifact 决定能不能跑，避免 readiness、manifest、validation 分散导致监控困难。

  P1：cheap proxy/full train 校准继续做实

  现在 proxy 是核心筛选器，但长期风险是 false positive。建议每次 full S3 后更新：

  - proxy delta vs full delta；
  - 哪个 dataset proxy 预测错；
  - 哪类 mechanism proxy 好但 full 差；
  - 当前 proxy threshold 是否需要放宽/收紧。

  这个能防止 cheap proxy 变成错误筛选器。

  P1：proxy 子集防过拟合

  如果同一个 cheap proxy subset 用太久，S2/S2.5 可能“适配 proxy”。可以做两层：

  - fixed mini subset：用于快速回归；
  - rotating holdout proxy subset：用于确认不是偶然提升。

  只有 fixed 好、rotating 不崩，才更值得进 full S3。

  P2：报告层再合并

  现在 artifact 很全，但人看还是会分散。auto-research report 可以重点显示：

  - 当前 direction；
  - 当前 candidate/fingerprint；
  - patch eligibility 状态；
  - implementation failure signals；
  - activation tensor diff；
  - proxy delta；
  - full status；
  - 下一步：S2.5 repair / S2 same-direction variant / S1 new direction。

  我建议下一步优先做 S2.5-only repair lane + 重复失败检测。这两个最直接降低 token 浪费，也最符合你现在的目标：先找到能跑、proxy 有收益、无评测污染的有效 patch。



 
  3. 增加 gradient-flow smoke
     第二个 patch 首 batch 报：
     element 0 of tensors does not require grad

     这个应该在 S2.5 前置检测：
      - loss.requires_grad
      - trainable parameter count
      - projector/aligner/router 是否有梯度
      - 新机制分支是否参与 loss
      - optimizer 是否真的拿到新参数

     这个比直接跑 full first batch 更能定位“代码接了但训练不动”。

  4. snapshot/worktree 干净性检查
     这次 S2.5 worktree 里有很多已有脏文件、coverage、docs、test 目录等，容易污染 diff 判断。
     应该加：
      - materialized snapshot 必须 clean；
      - .coverage/htmlcov/__pycache__ 自动忽略；
      - patch diff 只相对 clean snapshot 计算；
      - 如果 snapshot 本身 dirty，先 fail fast，别让 Codex 在脏基线上写。

  P1 优先级

  5. report 需要显示真实 blocked reason
     现在 auto-research report 还显示 Recent failure: none，但实际已经 blocked 在 S2.5 validation failure。
     应该让 report 直接显示：
      - 当前失败 candidate
      - failed check
      - repair dispatch
      - 下一步是否跳过 S2 planner
      - 当前是否 implementation failure

  6. S2.5 repair attempt 独立计数
     method 方向 5 次和 implementation repair 次数要完全分开。
     建议：
      - 每个 candidate 最多 3-5 次 S2.5 repair；
      - 连续同类 runtime failure 才标记 implementation_blocked；
      - 不回 S2，除非明确是 integration point 错了，不是代码实现错。

  7. patch 大小不要硬杀，但要分层处理
     现在 effect-first 没必要因为 diff 大直接杀。
     但可以分：
      - evaluator touched：硬拦
      - runtime 不通：S2.5 repair
      - diff 大但 runtime/probe 过：允许 cheap proxy，但记录 quality debt
      - diff 大且 no-op：S2.5 repair

  我不建议现在优先改 S1

  S1 现在能定方向，S2 也能生成结构化 variant。当前瓶颈不是 idea 不够，而是 patch 到不了 S3。先把 S2.5 runtime/repair/资源路由修顺，再去优化 idea 质量，收益更大。





