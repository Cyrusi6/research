{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "source",
    "identity",
    "next_action",
    "reason_codes",
    "budget_snapshot",
    "artifact_hashes",
    "idempotency_key"
  ],
  "properties": {
    "schema_version": {
      "const": "auto_research_route_outcome_v4"
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "event_id",
        "sequence",
        "attempt_id"
      ],
      "properties": {
        "event_id": {
          "type": "string",
          "minLength": 1
        },
        "sequence": {
          "type": "integer",
          "minimum": 1
        },
        "attempt_id": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "direction_id",
        "direction_semantic_hash",
        "direction_spec_hash",
        "variant_id",
        "variant_semantic_hash",
        "variant_spec_hash",
        "attempt_id"
      ],
      "properties": {
        "direction_id": {
          "type": "string",
          "minLength": 1
        },
        "direction_semantic_hash": {
          "$ref": "#/$defs/hash"
        },
        "direction_spec_hash": {
          "$ref": "#/$defs/hash"
        },
        "variant_id": {
          "type": "string",
          "minLength": 1
        },
        "variant_semantic_hash": {
          "$ref": "#/$defs/hash"
        },
        "variant_spec_hash": {
          "$ref": "#/$defs/hash"
        },
        "attempt_id": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "next_action": {
      "enum": [
        "IMPLEMENT_VARIANT",
        "REPAIR_IMPLEMENTATION",
        "RUN_METHOD_ATTEMPT",
        "PROPOSE_NEXT_VARIANT",
        "START_NEW_DIRECTION",
        "FINISH_DIRECTION",
        "FINISH_RUN",
        "PAUSE_RESOURCE",
        "BLOCK_INTEGRITY",
        "RUN_FULL"
      ]
    },
    "reason_codes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "budget_snapshot": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "target",
        "reserved",
        "consumed"
      ],
      "properties": {
        "target": {
          "const": 5
        },
        "reserved": {
          "type": "integer",
          "minimum": 0,
          "maximum": 5
        },
        "consumed": {
          "type": "integer",
          "minimum": 0,
          "maximum": 5
        }
      }
    },
    "artifact_hashes": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/hash"
      }
    },
    "idempotency_key": {
      "$ref": "#/$defs/hash"
    }
  },
  "$defs": {
    "hash": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    }
  }
}
